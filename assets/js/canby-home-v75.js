(()=>{'use strict';const els=[...document.querySelectorAll('.cinematic-reveal')];if(!els.length)return;const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;if(reduced){els.forEach(el=>el.classList.add('is-visible'));return;}const io=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('is-visible');io.unobserve(entry.target);}})},{threshold:.15,rootMargin:'0px 0px -4%'});els.forEach(el=>io.observe(el));})();